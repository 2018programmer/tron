<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dx.mapper.ChainAssetsMapper">
    <select id="getHaveAssets" resultType="com.dx.entity.ChainAssets">
        SELECT x.* FROM chain_assets x left join chain_coin c on x.coin_code =c.coin_code
            where x.balance >=c.threshold
            and x.asset_type = 2
            <if test="netName != null">
                and c.auto_gather = 1
            </if>
            <if test="netName != null">
                and x.net_name = #{netName}
            </if>
            <if test="coinName != null">
                and x.coin_Name = #{coinName}
            </if>
    </select>


</mapper>
